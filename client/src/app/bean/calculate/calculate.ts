export class Calculate {
  constructor(
    public productId                    ?: string,
    public noOfCartons                  ?: string,
    public noOfUnits                    ?: string,
    public selectedCarton               ?: boolean,
    public selectedUnit                 ?: boolean,
  ) {}
}
